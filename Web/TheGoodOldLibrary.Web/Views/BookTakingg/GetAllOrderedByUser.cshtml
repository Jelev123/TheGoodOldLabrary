@using TheGoodOldLibrary.Data.Models.ViewModel.BookTaking

@model List<AllTakingsBook>

@if (Model.Count > 0)
{
    <h5>User: @Model.FirstOrDefault().UserName</h5>

}


<h4>Taking</h4>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Take At</th>
        </tr>
    </thead>
    @foreach (var book in this.Model)
    {
        @if (book.IsTaken == true)
        {
            <tbody>
                <tr>
                    <td>@book.Id</td>
                    <td>@book.BookName</td>
                    <td>@book.CreatedOn</td>
                    <td>@book.UserName</td>
                    <td>
                        <a class="col-md-4" class="btn btn-danger" asp-controller="BookTakingg" asp-action="Return" asp-route-id="@book.Id">Return</a>


                    </td>
                </tr>
            </tbody>
        }


    }
</table>

<hr />

<h4>Returned</h4>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Returned At</th>
        </tr>
    </thead>

    @foreach (var book in this.Model)
    {

        @if (book.IsTaken == false)
        {
            <tbody>
                <tr>
                    <td>@book.Id</td>
                    <td>@book.BookName</td>
                    <td>@book.ReturnAt</td>
                </tr>
            </tbody>
        }


    }
</table>

